AM4 ALERT MASTER 3 - REQUESTED CHANGES
Date: 2026-07-23

HARDWARE ESP32
1. Display state JSON now sends the exact key:
   "Comp_status": comp_Status
2. All existing temperatures, pressures, outputs, time, authentication,
   heartbeat, and commands remain unchanged.

DISPLAY ESP32
1. Removed the ONLINE/OFFLINE visual badge implementation.
2. Dashboard top-left card is now TIME / DATE and displays:
   - Pakistan time
   - Date
   - Day abbreviation
3. Dashboard top-right card is now SYSTEM STATUS.
4. Display reads the exact hardware JSON key "Comp_status".
5. Status mapping requested by the user:
   Comp_status == 1 -> RUNNING
   Any other value -> STOPPED
6. Right-swipe navigation now toggles in both directions:
   Dashboard --right swipe--> Temperatures
   Temperatures --right swipe--> Dashboard
7. Key 4 and the bottom-right navigation target also toggle both pages.

STATIC VERIFICATION COMPLETED
- JSON producer and consumer use identical exact key: Comp_status
- No ONLINE or OFFLINE UI strings remain in display source
- All source/header brace counts are balanced
- No stale calls to am4DrawHeader(bool) remain
- Dashboard and temperature page drawing paths remain intact
- Existing link protocol, sensor values, pressure values, and remote time flow remain intact

NOTE
A physical compile/upload test still requires the Arduino IDE libraries and the
actual two ESP32 boards. Use the existing ARDUINO_IDE_TEST_STEPS.txt for board
and library setup.
