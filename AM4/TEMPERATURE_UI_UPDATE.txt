AM4 TEMPERATURE PAGE UI UPDATE

Updated file:
- AM4_DISPLAY/AM4_UI.h

Changes:
1. Rebuilt the temperature page as six evenly spaced 304 x 26 pixel cards.
2. Made RETURN TEMP, SUCTION TEMP, DISCHARGE TEMP, SUPPLY TEMP and OIL TEMP visually bold.
3. Kept the standard Adafruit GFX font; no extra font library is required.
4. Enlarged and centered each live temperature value area.
5. Added a clearer double divider between each temperature name and its value.
6. Improved thermometer icon alignment.
7. Changed the selected sensor highlight to a visible two-colour, two-pixel border.
8. Restored the row shadow correctly after keypad selection changes.
9. Added index bounds checks to all temperature-row drawing functions.

Hardware JSON, Comp_status handling, pressure display, time/date display and swipe navigation were not changed.

Verification performed:
- C++17 syntax-only compilation of AM4_UI.h with Arduino/Adafruit-compatible interface stubs: PASS.
- Delimiter and brace validation: PASS.
- Temperature-page geometry: PASS (last row ends at Y=221; footer starts at Y=228).
- Live value area width: 90 pixels, sufficient for values such as -99.9 C.
- Confirmed hardware sender still uses JSON key Comp_status.
- Confirmed display receiver still reads JSON key Comp_status.

AM4 addition: the sixth row is Other Sensor and receives otherC from the hardware link.
