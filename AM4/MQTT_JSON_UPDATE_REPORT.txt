MQTT JSON UPDATE REPORT
=======================

Updated only the hardware-host MQTT/mobile-app communication layer.
The display firmware, TFT UI, touch swipe, keypad navigation, and direct
hardware-display TCP protocol were not changed.

Implemented:
- Exact publish topic: /KRC/AM4-AAA001
- Exact temperature-config topic: /KRC/AM4-AAA001/temperature_config_AM4
- Exact setpoint receive topic: /test/AM4-AAA001/1
- Exact control/config receive topic: /test/AM4-AAA001/2
- Added oilsp to telemetry, MQTT receive handling, Preferences storage, and boot loading
- Added returnTempSelection to app protocol compatibility storage and config output
- Removed resetsw from the published temperature-config payload
- Preserved valid fifth oil sensor role code 5
- Kept pressure *F keys as bar values for existing app compatibility
- Kept all requested live telemetry network fields

Verification:
- MQTT.h passes GCC and Clang C++17 syntax checks using Arduino-compatible mocks
- Required outgoing telemetry keys verified
- Required outgoing temperature-config keys verified
- Required /1 and /2 receive keys verified
- Topics verified
- Display project confirmed unchanged byte-for-byte
- Corrected protocol examples parse as valid JSON

Latest key correction:
- Other Sensor telemetry now uses the exact app keys othertemp and othertempF
- Hardware/display private key remains otherC on both endpoints
